<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ZeusApp"
        parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/shark</item>
        <item name="colorPrimaryVariant">@color/woodsmoke</item>
        <item name="colorOnPrimary">@color/black_squeeze</item>
        <item name="colorAccent">@color/mariner</item>
        <item name="colorSurface">@color/blue_grey_900</item>
        <item name="colorOnSurface">@color/black_squeeze</item>
        <item name="android:windowBackground">@color/woodsmoke</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor"
            tools:targetApi="l">?attr/colorPrimaryVariant
        </item>
    </style>
</resources>